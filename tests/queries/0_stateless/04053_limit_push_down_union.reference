Expression (Project names)
  Limit (preliminary LIMIT)
    Union
      Limit
        Expression ((Projection + (Change column names to column identifiers + (Project names + (Projection + Change column names to column identifiers)))))
          ReadFromSystemNumbers
      Limit
        Expression ((Projection + (Change column names to column identifiers + (Project names + (Projection + Change column names to column identifiers)))))
          ReadFromSystemNumbers
---
Expression (Project names)
  Limit (preliminary LIMIT)
    Union
      Limit
        Expression ((Projection + (Change column names to column identifiers + (Project names + (Projection + Change column names to column identifiers)))))
          ReadFromSystemNumbers
      Limit
        Expression ((Projection + (Change column names to column identifiers + (Project names + (Projection + Change column names to column identifiers)))))
          ReadFromSystemNumbers
---
Expression (Project names)
  Limit (preliminary LIMIT)
    Union
      Limit
        Expression ((Projection + (Change column names to column identifiers + (Project names + (Projection + Change column names to column identifiers)))))
          ReadFromSystemNumbers
      Limit
        Expression ((Projection + (Change column names to column identifiers + (Project names + (Projection + Change column names to column identifiers)))))
          ReadFromSystemNumbers
      Limit
        Expression ((Projection + (Change column names to column identifiers + (Project names + (Projection + Change column names to column identifiers)))))
          ReadFromSystemNumbers
---
5
---
3
---
Expression (Project names)
  Limit (LIMIT WITH TIES)
    Sorting (Sorting for ORDER BY)
      Union
        Expression ((Before ORDER BY + (Projection + (Change column names to column identifiers + (Project names + (Projection + Change column names to column identifiers))))))
          ReadFromSystemNumbers
        Expression ((Before ORDER BY + (Projection + (Change column names to column identifiers + (Project names + (Projection + Change column names to column identifiers))))))
          ReadFromSystemNumbers
---
Expression (Project names)
  Limit
    Filling
      Sorting (Sorting for ORDER BY)
        Union
          Expression ((Before ORDER BY + (Projection + (Change column names to column identifiers + (Project names + (Projection + Change column names to column identifiers))))))
            ReadFromSystemNumbers
          Expression ((Before ORDER BY + (Projection + (Change column names to column identifiers + (Project names + (Projection + Change column names to column identifiers))))))
            ReadFromSystemNumbers
