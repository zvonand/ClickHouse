#include <Common/Coverage.h>

/// This file was previously used to dump client-side coverage via CLICKHOUSE_WRITE_COVERAGE.
/// That mechanism has been removed in favour of server-side per-test coverage via
/// system.coverage_log and SYSTEM SET COVERAGE TEST.
