no_row_policy
    (ReadFromMergeTree)
    Concat 4 → 1
      MergeTreeSelect(pool: ReadPoolInOrder, algorithm: InOrder) × 4 0 → 1
row_policy
    (ReadFromMergeTree)
    MergeTreeSelect(pool: ReadPool, algorithm: Thread) × 4 0 → 1
row_policy_always_true
    (ReadFromMergeTree)
    Concat 4 → 1
      MergeTreeSelect(pool: ReadPoolInOrder, algorithm: InOrder) × 4 0 → 1
additional_filter
      (ReadFromMergeTree)
      MergeTreeSelect(pool: ReadPool, algorithm: Thread) × 4 0 → 1
